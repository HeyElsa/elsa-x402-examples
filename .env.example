# Elsa X402 Examples - Environment Variables

# Required: Your wallet private key (with USDC on Base for paid endpoints)
# Must be a valid hex string: 0x + 64 hex characters
PRIVATE_KEY="0x..."

# Required: Wallet address to use for testing API endpoints
# Must be a valid Ethereum address: 0x + 40 hex characters
TEST_WALLET="0x..."

# Required: Use mainnet (true) or testnet (false)
USE_MAINNET="true"

# Required for mainnet: Base RPC URL
BASE_RPC_URL="https://mainnet.base.org"

# Required for testnet: Base Sepolia RPC URL
BASE_SEPOLIA_RPC_URL="https://sepolia.base.org"

# Optional: Override the default API server
X402_SERVER_URL="https://x402-api.heyelsa.ai"
